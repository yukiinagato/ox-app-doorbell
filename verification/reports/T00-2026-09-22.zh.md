# T00 执行报告

日期：2026-09-22 18:31 JST  
执行者/独立审查者：Codex / 独立复核未执行  
任务状态：VERIFIED_IN_SCOPE

## 来源与范围

审查参照：`b26e0d346df99f2879915241b6e98d8ef1aa0707`  
执行前 HEAD：`b26e0d346df99f2879915241b6e98d8ef1aa0707`  
验证来源 SHA：同上  
工作区：执行前 clean；输出报告和日志后仅新增 T00 证据文件  
`BASE..HEAD`：0 个文件；空差异 SHA-256 为 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`

本轮只做 T00 inventory，没有修改生产代码、测试、资源、ABI、最低系统版本、应用版本/build 或 `ios-legacy`。没有 push、tag、release、部署、签名上传、真实门锁或 SOS。

## 真实生产入口

已核实的入口包括：Core ABI (`core/include/doorbell/doorbell.h`、`core/src/capi/doorbell_capi.cpp`)、Core HTTP/状态 (`core/src/node/node.cpp`)、HTTP run loop (`core/src/httpd/httpd.cpp:runOnLoop`)、Web panel (`call.html`、`runtime.js`、`video-session.js`)、Web admin (`admin/app.js`)、现代 iOS CoreBridge/时钟/呼叫壳、Android `MainActivity`、Windows `MainWindow` 和 `ios-kiosk` Objective-C 壳。Q 项位置和调用责任见 `issue-triage.json`。

## 结果

- 当前 HEAD 与审查参照相同；没有后续差异需要回退或重做。当前 HEAD 的既有 M01–M08 修复保留，并按 host/Node/交叉编译证据层记录。
- Q01–Q09、Q11–Q18：`CONFIRMED_STATIC`。这是当前源码审查结论，不是动态事故已发生的声明。
- Q04：代码中确认的通用 HA 回退设计风险；本轮未触发开门。
- Q12：写入契约缺口；本轮未制造并发编辑。
- Q10：`NEEDS_REPRODUCTION`，仍是尚未动态复现的并发风险；本轮未写成已观察到的崩溃/UAF。
- R1–R6 的 55 个历史场景继续保持未由本轮复核者运行；既有 C/W/V/B 证据没有被提升为设备证据。

## 文件变化

| 文件 | 原因 |
|---|---|
| 计划目录 `baseline.zh.md` | 固化当前 HEAD、入口、既有修复、工具链和阻塞。 |
| 计划目录 `issue-triage.json` | 固化 Q01–Q18 状态、位置、任务映射和历史保护。 |
| 计划目录 `platform-inventory.json` | 固化平台、工具链、SDK、设备和签名缺口。 |
| `verification/logs/t00-baseline-2026-09-22.log` | 记录本轮只读检查的退出码和 NOT_RUN 边界。 |
| `verification/reports/T00-2026-09-22.zh.md` | 本任务报告。 |
| 计划目录 `progress.json` | 仅更新 T00 状态、证据路径和下一入口。 |

没有版本/build 变化：本轮无 shippable application source/resource 变更。

## 验证

| test_id | 证据层 | command / cwd | exit_code | 实际结果 | 日志 |
|---|---|---|---:|---|---|
| T00-01 | git/workspace | `git status --short`；`/Users/ox/Documents/project/app-doorbell` | 0 | PASS；执行前 clean，无用户修改；未 reset/stash/clean | `verification/logs/t00-baseline-2026-09-22.log` |
| T00-02 | git/object/source | `git cat-file -e BASE^{commit}`、`git diff BASE..HEAD`；项目根 | 0 | PASS；参照对象存在，HEAD 保持不变，BASE..HEAD 为空 | 同上 |
| T00-03 | static triage | 读取 `source/findings.json`、当前符号和 Q10 Bridge 路径；项目根/计划目录 | 0 | PASS；Q10 标为未动态复现风险，动态验证仍 NOT_RUN | 同上 |
| T00-04 | platform inventory | Xcode/SDK/ADB/本地依赖只读探测；项目根 | 0 | PASS；iOS9 armv7、Android、目标设备等缺口已记录，未以 arm64/host 替代 | 同上 |

下列验证均是 `NOT_RUN`：Core/CTest、Android modern/legacy19 Gradle、现代 iOS/tvOS App 构建与签名、iOS9 arm64/armv7 App、iOS5 App/设备、Windows native/WPF、真实认证浏览器、双节点 HTTPS/摄像头/WebRTC、真实 SIP/媒体、Q01–Q18 动态反例、Q10 屏障竞态、物理门锁/SOS。既有日志只作为历史证据引用，没有冒充本轮运行。

## 原有行为保护与阻塞

已保护 Core ABI、旧平台边界、`ios-legacy` 冻结、R1/R3/R6/C01–C03/W01–W04/V01/B1–B4/M01–M08 追踪，以及“host/Node/交叉构建不等于设备”的证据分层。应用版本/build 保持原值：Android `0.3.18+<git7>`/19，现代 iOS 0.1.34/35，iOS5 kiosk 0.3.46/49，iOS9 armv7 profile 0.4.0/400。

阻塞：本机没有可用 Java runtime 或 Android SDK/NDK 条目；没有历史 Xcode 9/5.1 SDK，正式 iOS9 armv7 PJSIP root 缺失；没有 Android/Apple 真机；没有 Windows native/WPF 环境；没有认证双节点浏览器和摄像头/WebRTC。Q10 需要后续屏障复现。上述是环境/证据缺口，不是产品逻辑 PASS。

## 独立复核

未执行独立复核，故不把 T00 标为已独立审查；报告中的 T00 状态仅覆盖本轮 inventory。未完成项保持 `NOT_RUN` 或 `BLOCKED_ENV`。

## 下一步

仅进入 T01：从当前完整 HEAD 建立真实生产路径测试入口和基线结果，使用独立 build 目录，先验证命令/target 是否存在，再按 host、浏览器、签名安装、设备分别记录证据。完成本报告后停止。
