# main 复核 M01–M08：执行报告

基准与起点：`e76100b7582ef7dd4b898c881c36d2f0b5046373`；开始时 HEAD 等于该 SHA，工作区 clean。历史 `verification/r1-r6-traceability.json` 保持原样（其来源 SHA 为 `0955c...`），本轮状态见 `verification/review-main-e76100b-overlay.json`。本轮未 push、合并、发版、部署或操作设备。

初版报告未找到 `findings.json`；其后用户提供完整压缩包，已验证并读取。逐场景覆盖与未运行项以 `verification/review-main-e76100b-findings-coverage.json` 和 `verification/reports/review-main-e76100b-findings-addendum.zh.md` 为准。

## 实现与正式回归

| 事项 | 实现 | 生产路径回归 | 实际结果 |
| --- | --- | --- | --- |
| M01 | Windows 改用 FLS，POSIX 保留 pthread key；所有 TLS 创建/设置/栈分配失败都会跳过回调而非进入未追踪的自注销死锁。 | `core/src/capi/doorbell_capi.cpp`；`[R2]` C ABI 回归与 iOS5 Core 构建。 | PASS（host、iOS5 armv7 静态 Core） |
| M02 | call recovery 捕获 binding 与 session，只结束仍为当前的那一对。 | `webui/panel/call.html`；`webui/tests/call_recovery_controller.test.js` 直接提取并运行生产控制器函数。 | PASS（Node VM；非真实浏览器） |
| M03 | 摄像头失败回传 owner binding，页面只清空匹配的 `videoBinding`，从而可重试。 | `webui/panel/video-session.js` 与 `call.html`；`webui/tests/video_session.test.js`。 | PASS（Node；无真实相机） |
| M04 | page epoch 限定 probe/login/boot；500 有可见错误和至多三次退避重试；pair tab visit token 与 XHR abort intent 已接线。 | `webui/admin/app.js`；`webui/tests/admin_runtime.test.js` 直接加载生产入口。 | PASS（Node VM；无已认证浏览器） |
| M05 | H.264 参数集先暂存；仅有效 SPS/PPS 且 IDR 引用对应 PPS 时换代，非法更新保留旧配置且不发布不可信 AU。 | `core/src/media/video_track.cpp`；`core/tests/test_fmp4.cpp [B3][M05]`，并把生产输出输入 fMP4 软件 demux。 | PASS（host；非目标播放器解码） |
| M06 | NTP 测试以可计数、绝不回复的 loopback 黑洞确认新 UDP 请求，等待与生产调度分离。 | `core/tests/test_time.cpp`。 | PASS |
| M07 | NTP desired config generation 与 worker round 分离；旧结果被丢弃，完成后立即运行最新 server；仅 interval 变更仍不立即发包。 | `core/src/node/node.cpp`、`core/tests/test_time.cpp`。 | PASS |
| M08 | 新增当前 overlay，更新 production entrypoint 与 parity 当前基准，不改写历史 55 场景。 | `verification/review-main-e76100b-overlay.json`、`verification/production-entrypoints.json`、`feature-parity.json`。 | PASS（JSON 校验） |

应用交付物版本已按仓库约定递增：Android `0.3.18`/19，现代 iOS `0.1.34`/35，iOS5 compatibility `0.3.46`/49。

## 命令、日志与资格

详细命令、退出码和汇总日志在 `verification/logs/review-main-e76100b-summary.log`。完整 host CTest 实测为 413 case、10639 assertion、0 failure；该次全量结果位于 `build/review3-host/Testing/Temporary/LastTest.log`。

Windows x64 MinGW 交叉 Core DLL 已生成；Windows x86 的 M01 C ABI 翻译单元已生成 Intel 80386 COFF 对象，但完整 x86 Core 在进入本轮代码前被既有 CivetWeb `InterlockedIncrement`/`InterlockedDecrement` 指针类型不兼容阻塞。两者都不是 Windows native runner、WPF 或运行时资格，故不宣称 Windows 平台 PASS。iPad mini 1 / iOS 9.3.6 / armv7 为 NOT RUN：没有签名 lane/连接设备；iOS5 armv7 PJSIP-off Core 静态库 PASS 不等于该设备或 miniSIP 媒体资格。真实浏览器、摄像头、WebRTC、目标 H.264 decoder 均未验证，不宣称 PASS。

当前追踪的源变更（不含 verification 文件）SHA-256 为 `fdb8545866390811c7f659e78547f2437773b85b6735b8b03a3345bddbdd70cf`；未跟踪的正式测试文件为 `webui/tests/call_recovery_controller.test.js`，在提交前须纳入索引。
