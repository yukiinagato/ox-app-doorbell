# 2026-09-22 并行修复批次报告

状态：T03/T09 已在 production_test 范围验证，T20 已在 native_lifecycle 范围验证；三张卡的计划状态均已更新为 VERIFIED_IN_SCOPE。加上此前 T00–T02，目前 6/50 张卡在各自范围完成。
整体计划仍为 NOT_READY；本报告不代表两个 ZIP 的全部问题已经修完。

## 本批改动

- T03：开门只执行该门显式绑定的命令，不再借用第一个 SIP/HA 动作；管理后台支持明确绑定、迁移提示与清空解绑。
- T09：共享请求支持有界等待、取消与代次；首先迁移通话信息请求，并修复独立复核发现的“状态先失败导致下一轮卡住”。
- T20：整段原生调用持有 Core 租约，停机等待与销毁移到后台；重置/角色切换等待完成，旧帧、UI 和时钟结果按代次隔离。原子代次准入、重置与排队重启的交错、同一 Core 内重建采集/编码实例的旧故障回调均已补齐回归。
- 用户追加的多门同时呼入：完成中英设计与独立源码复核，归入 T36。iPad 主通话加侧栏等待、iPhone 紧凑等待列表；动作目标稳定、明确选择下一路、首版等待项只显示文字。MC-01–12 均未运行，界面未实施。

## 已取得的验证

- Core：全量 419 用例、10852 断言通过；T09 页面补修和注释清理后重建，T03 定向 4 用例/250 断言再通过。
- Web：最终候选全部 14 套脚本通过；真实 Core HTTP 夹具提供的 admin/app.js、runtime.js 和 call 页面与来源逐字节相同。
- Android：modern/API21/NDK27 与 legacy19/API19/NDK25 分目录构建及 lint 通过；各通道 43 套/377 条 JVM 结果复用 Gradle 有效缓存，不声称本次重新执行 754 条。APK 为 0.3.20，build21。
- iOS5：本地历史 SDK 的 Core 与 App 构建通过，0.3.48/build51；没有安装到设备或触发门锁。
- iOS 原生：最终全套 143 项测试通过，其中含 13 项生命周期/生产者状态专项；同一组 13 项分别通过 ASan/TSan，不把重复执行算成新覆盖。插桩仅覆盖 Swift/Objective-C 宿主，自定义 C++ Core 静态库未传播插桩选项。iOS Simulator 测试采用明确允许的 Debug SIP stub。
- tvOS Simulator：最终 Debug 构建通过，链接真实 PJSIP；尚未执行 tvOS 设备测试。
- iOS9 arm64：最终无签名 Release 构建和产物检查通过（最低 iOS9、ABI v2、真实 PJSIP）；当前 Xcode 对 iOS9 目标仍有支持范围提示，不把构建当成旧机运行验收。
- 兼容 host：最后一轮完整通过，包括修正后的停机/重启源契约；构建与源契约不替代硬件测试。
- 翻译生成、一致性、英文源与差异空白检查已通过，最终来源检查也已通过。

精确来源、命令、日志、版本和产物摘要分别位于 source-candidate-*.json、evidence/、versions.json 和 evidence/artifacts.json。
每次记录保留原始失败，不覆盖旧日志。版本在本批每个受影响交付物中只增加一次。

## 独立复核发现及处理

T03 删除了误导管理员无需迁移的陈旧注释。T09 用真实页面反例捕获了早期失败与请求所有权问题并修正。
T20 的独立复核发现旧帧跨重启准入、重置被排队身份重启覆盖、旧采集/编码状态回调污染；三个问题已闭环，最终独立审查及证据见 T20/independent-review.md 与 T20/acceptance.json。

## 尚未取得的资格

- Chrome 被扩展弹窗阻止自动化，IAB 未连接；新管理表单的真实浏览器操作未完成。生产脚本主机测试和 HTTP 资源验证不替代此项。
- 真实门锁、MQTT 执行器 ACK、双节点网络及 iPad/iPhone/旧系统设备资格均不由主机构建证明。
- 多来电设计仍需既有 T36 前置完成，尤其按住户限定的接收/恢复资格、操作身份与媒体权限。没有把全局 active_calls 当作所有住户的来电队列。
- iOS9 armv7 正式历史工具链/签名通道与 iOS5、iOS9 arm64 分开；Windows 原生运行验证尚未执行。

本任务未推送代码、标签或发布，也未部署到用户设备。

## 来源保全与清理

最终来源清单为 source-final.json；实际最终编译输入与 source-candidate-r4.json 相同。
1356 个进入本批时的文件未改动，43 个文件在本批明确范围内变化，没有删除文件或改动 ios-legacy。
收尾期间工作区出现外部 AGENTS.md 和三语 test-devices.md 更新，已读取并原样保留，未算作本批修复，
也没有据此连接或部署设备；见 evidence/preservation.json。

两次 HTTP 夹具均已退出，端口和数据库锁释放，自有测试数据已清理。T20 专用 Simulator 已回读确认为 Shutdown，
自有临时测试目录剩余 0；测试结果、App 和逐运行 native 归档副本保留供复核。
