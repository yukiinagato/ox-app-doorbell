# T30 只读准备

状态：PREPARATION_ONLY；T25 已完成，T29 独立复核与最终冻结待主任务确认。本文件不表示 T30 生产 UI 已实施或通过。

现有 System 页导出直接下载裸 `S.cfg`；导入接受裸对象、entries 对象或数组，展平后调用普通 `postEntries`，继承 256 项限制。没有暂存/预检、字段级删除确认、导入操作句柄或独立恢复查询。原始源码已冻结到 `baseline-source.json`。现阶段没有修改生产代码、i18n 或进度。

准备中的有界实现：

1. System 导入卡独立状态机，复用 T25 的当前鉴权代次、超时和页面退休规则；不改 T27 普通编辑器、T28 冲突解决或后台快照刷新。
2. 普通导出增加 schema、内容范围、源节点/应用版本元数据；输出配置和 secret reference，去除密码/令牌/Push 明文。继续明确“配置导出不是完整灾难恢复备份”，资源与安全存储分开恢复。
3. 接受新完整快照，以及原裸配置/entries 合法格式。旧格式默认按当前快照合并转换，不默许删除所有缺失实体；完整替换必须明确选择并检查预检的删除列表。提交前显示节点、文件摘要、条目数、差异和删除风险。
4. 使用 T29 的 stage → preflight → 明确 commit；预检 can_commit 为 false 时禁用提交，列出缺失引用/资源及问题。不得分批绕过 256 普通 batch 限额。
5. 保存原 operation_id、token、digest 的有界恢复上下文；提交超时/断网只提供原操作查询，绝不另建操作或自动重送。主任务确认 T29 将补只读 `/api/config/import/query`，请求 schema2、operation_id、stage_token、digest；committed 返回原 result，operation_not_found 不能自动重新提交。
6. 原文件与敏感输入不写浏览器持久存储。401 清除原始 JSON/秘密输入，保留非敏感摘要和既有操作句柄，重新鉴权后显式查询。secret 引用灌注复用现有安全存储接口，并显式提示随后需刷新快照重新预检。
7. 成功仅称本节点持久化成功；刷新真实 status 的同步信息，不捏造“全部设备恢复完成”。

拟拥有 `webui/admin/app.js` 的 System 导入/导出区和新增测试，必要英文导入说明及日/中译文。i18n 单一来源新 keys 将交主任务统一生成。暂不改 Core、普通编辑器、版本或任务进度。

验证准备：真实 IAB 生产 DOM 受控网络测试 257 项预检/提交、删除明确确认、缺 secret 禁止提交、401/超时保留摘要/句柄、commit 已执行但丢响应后的只读 query、重复点按单飞；再以冻结 T29 Core 实际 HTTP 夹具联测 257 项完整往返与 receipt 查询。受控 XHR 不能冒充端到端。普通导出专测密码/令牌/Push 明文不出现在下载内容。基线 DOM/截图与红例将在依赖明确后记录。
