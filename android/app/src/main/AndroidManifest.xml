<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- メッシュ通信 + 管理 webui -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <!-- 訪客カメラ / 通話・監聴マイク (SIP) -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- 常駐 (core をプロセスごと生かす前台サービス) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- TV: 前台アプリの上に来客モニタ画面を被せる (バックグラウンド起動の免除条件。
         付与は adb: appops set jp.keihan.doorbell SYSTEM_ALERT_WINDOW allow — provision.md) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <!-- カメラ無し端末 (通知専用ノード転用) にも入れられるように required=false -->
    <uses-feature android:name="android.hardware.camera.any" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.front" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>
    <!-- Android TV (Leanback): 室内監視器として同一 APK を入れる。タッチ非必須 -->
    <uses-feature android:name="android.software.leanback" android:required="false"/>
    <uses-feature android:name="android.hardware.touchscreen" android:required="false"/>

    <application
        android:name=".App"
        android:allowBackup="false"
        android:banner="@drawable/banner_tv"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.Doorbell"
        android:usesCleartextTraffic="true">
        <!-- 平文 HTTP は LAN 内に閉じた用途のみ: 門口ライブ (MJPEG)・自機 httpd の
             /asset/<hash> (テーマ背景/カスタム音声)。targetSdk 28+ の既定 (禁止) では
             どちらも接続できない。 -->

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:excludeFromRecents="true"
            android:exported="true"
            android:launchMode="singleTask"
            android:lockTaskMode="if_whitelisted">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- TV ランチャー (Leanback) からの起動 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
            <!-- kiosk: HOME に設定して起動器を置き換える (provision.md) -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.HOME"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <!-- 来客モニタ画面: chime イベントで前台に被さる (門口ライブ映像 + 監聴 + クイック返信)。
             D-pad (TV リモコン) とタッチの両対応。 -->
        <activity
            android:name=".IncomingActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:turnScreenOn="true"/>

        <!-- 常駐前台サービス: 通知 1 本で core プロセスを生かし続ける (TV/タブレット共通) -->
        <service
            android:name=".DoorbellService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="ドアホン子機の常駐 (メッシュ待受・来客通知・SIP 監聴)"/>
        </service>

        <!-- Device Owner 用 admin (adb: dpm set-device-owner jp.keihan.doorbell/.AdminReceiver) -->
        <receiver
            android:name=".AdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin"/>
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED"/>
            </intent-filter>
        </receiver>

        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
    </application>
</manifest>
